# 元征 · 合伙人赋能平台 - 后端环境变量模板
# 复制此文件为 .env 并填写实际值

# ==========================================
# 服务配置
# ==========================================
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# ==========================================
# 数据库配置
# ==========================================
DATABASE_URL="postgresql://yuanzheng:yuanzheng123@localhost:5432/yuanzheng_db?schema=public"

# ==========================================
# JWT配置
# ==========================================
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ==========================================
# 日志配置
# ==========================================
LOG_LEVEL=info

# ==========================================
# CORS配置
# ==========================================
CORS_ORIGIN=http://localhost:5173

# ==========================================
# SMTP邮件配置 (PRD D: 强触达通知)
# ==========================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
SMTP_FROM="元征平台 <noreply@yuanzheng.com>"

# ==========================================
# Token 初始额度 (PRD v2/v3)
# ==========================================
TOKEN_INITIAL_FOUNDER=100000
TOKEN_INITIAL_CORE_PARTNER=30000
TOKEN_INITIAL_PARTNER=10000

# ==========================================
# 座谈会嘉宾邀请奖励默认值 (PRD 4.2, C)
# ==========================================
REWARD_PUBLIC_CO_DMHG=500
REWARD_FIN_EXEC=500
REWARD_PUBLIC_CHAIRMAN=1000
REWARD_MINISTRY_LEADER=2000
REWARD_OTHER=0

# ==========================================
# 验证码配置 (PRD 8.2)
# ==========================================
VERIFICATION_CODE_TTL=600
VERIFICATION_CODE_INTERVAL=60

# ==========================================
# 登录安全配置 (PRD 8.2)
# ==========================================
LOGIN_MAX_ATTEMPTS=5
LOGIN_WINDOW_MINUTES=5

# ==========================================
# 性能配置 (PRD 4.5)
# ==========================================
PERF_NORMAL_API=300
PERF_EXTERNAL_API=3000

# ==========================================
# 功能开关 (PRD 可配置项)
# ==========================================
FEATURE_FOUNDER_CREATE_MEETING=false
FEATURE_SELF_REGISTRATION=false

# ==========================================
# 场地预约配置 (PRD 19.5.2)
# ==========================================
MEAL_TIME_BUFFER=30

# ==========================================
# 邮件频控配置（秒）
# ==========================================
EMAIL_DM_BATCH=60
EMAIL_COMMUNITY_BATCH=300

# ==========================================
# 备份配置 (PRD 4.5)
# ==========================================
BACKUP_RETENTION_DAYS=30
BACKUP_DIRECTORY=/var/backups/yuanzheng

# ==========================================
# 超级管理员配置（用于首次部署）
# 注意：生产环境部署完成后建议关闭
# ==========================================
SUPER_ADMIN_ENABLED=true
SUPER_ADMIN_IDENTIFIER=admin@yuanzheng.com
SUPER_ADMIN_PASSWORD=Admin@123456
SUPER_ADMIN_NAME=超级管理员
